/**
 * A generated `AsteroidEditorExtension` example...
 *
 * Examples should show a working module api
 * and be used in tests to continously check
 * they function as expected.
 */
 
var AsteroidEditorExtension = require('../');
var asteroidEditorExtension = AsteroidEditorExtension.create();

asteroidEditorExtension.myMethod();