/**
 * Expose `LoopbackEditorGenerator`.
 */

module.exports = LoopbackEditorGenerator;

/**
 * Module dependencies.
 */
 
var loopback = require('loopback')
  , EventEmitter = require('events').EventEmitter
  , LoopbackEditorExtension = require('loopback-editor-extension')
  , debug = require('debug')('loopback-editor-generator')
  , util = require('util')
  , inherits = util.inherits
  , assert = require('assert');
  
/**
 * Create a new `LoopbackEditorGenerator` with the given `options`.
 *
 * @param {Object} options
 * @return {LoopbackEditorGenerator}
 */

function LoopbackEditorGenerator(options) {
  options.layout = options.layout || '/layouts/editor-layout.ejs';
  LoopbackEditorExtension.apply(this, arguments);
  
  this.options = options || {};

  
}

/**
 * Inherit from `EventEmitter`.
 */

inherits(LoopbackEditorGenerator, LoopbackEditorExtension);

/**
 * Simplified APIs
 */

LoopbackEditorGenerator.create =
LoopbackEditorGenerator.createLoopbackEditorGenerator = function () {
  // add simplified construction / sugar here
  return new LoopbackEditorGenerator();
}

/**
 * Methods.
 */

