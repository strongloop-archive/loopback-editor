/**
 * A generated `AsteroidEditorUtility` example...
 *
 * Examples should show a working module api
 * and be used in tests to continously check
 * they function as expected.
 */
 
var AsteroidEditorUtility = require('../');
var asteroidEditorUtility = AsteroidEditorUtility.create();

asteroidEditorUtility.myMethod();