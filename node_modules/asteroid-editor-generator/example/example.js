/**
 * A generated `AsteroidEditorGenerator` example...
 *
 * Examples should show a working module api
 * and be used in tests to continously check
 * they function as expected.
 */
 
var AsteroidEditorGenerator = require('../');
var asteroidEditorGenerator = AsteroidEditorGenerator.create();

asteroidEditorGenerator.myMethod();